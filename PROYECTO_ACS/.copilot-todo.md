- [ ] Reproduce `validate_dataguard.sh` output on standby to capture current switchover status and MRP state.
- [ ] Inspect `oracle_shared_volume` inside containers to confirm archivelogs and backup artifacts are generated as expected.
- [ ] Track down the `mv ... tnsnames.ora: Permission denied` errors by reviewing init logs and verifying mount permissions.
